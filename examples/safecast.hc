# Placed on an item such as a Trinket to execute the block stored on the item in other hand

LDACCESS     # guard to prevent trying to read unreadable items and dropping both
[ LD EXEC ]
[ ]
IF_ELSE EXEC